---
- shell:
    cmd: |
      set -e
      set -x

      oc cluster up
      oc login -u developer -p developer
    executable: /bin/bash
  become: true
  delegate_to: "primary"
  environment: '{{ zuul | zuul_legacy_vars }}'
  register: oc_output
