coe_host:
coe_config_context:
coe_config_file:
action: provision

service_name: mariadb
namespace: openstack

hiera_data: {}
hiera_data_file: ''

mariadb_config:
  host: '{{service_name}}'
  root_password: weakpassword
